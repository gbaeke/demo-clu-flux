apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: influxdb
  namespace: default
spec:
  releaseName: redis
  chart:
    repository: https://charts.bitnami.com/bitnami
    name: influxdb
    version: 0.2.4